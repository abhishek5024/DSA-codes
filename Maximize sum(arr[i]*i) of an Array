sort(a, a+n);
       
       long s=0;
       int mod=1000000007;
       int i;
       for (i=0;i<n;i++){
       
            s = (s+(long)a[i]*i)%mod;
       
       }
       
       return s;
       return 0;
